[project]
name = "agent"
version = "0.1.0"
description = "Simple Telegram bot to run Python files remotely and return output or errors."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytelegrambotapi>=4.34.0",
]
